#!/bin/bash

if [ "$(uname)"=="Darwin" ];then
  alias ls='ls -G'
else
  alias ls='ls --color=auto'
fi

alias sl='ls'
alias l='ls'
alias s='ls'
alias ll='ls -shlF'

alias mkdir='mkdir -v'
alias mv='mv -v'
alias cp='cp -v'
alias rm='rm -v'
alias ln='ln -v'

# vim
alias vi=nvim
alias vim=nvim

